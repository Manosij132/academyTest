/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PrivilegedUserDirective } from './privileged-user.directive';

describe('Directive: PrivilegedUser', () => {
  it('should create an instance', () => {
    const directive = new PrivilegedUserDirective();
    expect(directive).toBeTruthy();
  });
});
